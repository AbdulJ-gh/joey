import test from 'ava';
// import { Config } from '../../config';
// import { sizeLimit } from '../sizeLimit';
//
// const config = {
// 	maxUrlLength: 30,
// 	maxQueryLength: 15
// } as Required<Config>;
//
// test('sizeLimit - URL within limit', t => {
// 	const url = new URL('https://a.com');
// 	t.is(sizeLimit(url, config), null);
// });
//
// test('sizeLimit - URL above limit', t => {
// 	const url = new URL('https://abcdefghijklmnopqrs.com');
// 	t.is(sizeLimit(url, config), null);
// });

test('sizeLimit - PASS', t => {
	t.pass();
});
